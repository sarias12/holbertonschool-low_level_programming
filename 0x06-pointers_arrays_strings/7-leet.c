#include <stdio.h>
#include "holberton.h"
/**
  * *leet - function that encodes a string into 1337.
  *
  * @s: string
  *
  *
  * Return: String Upper
  */
char *leet(char *s)
{
	int i;
	char letter[]= { 'a', 'e', 'o', 't', 'l'};
	char numbs[]




	for (i = 0 ; s[i] < '\0' ; i++)
		if ()

	return (s);
}
